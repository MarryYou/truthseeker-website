"""筛选清洗阶段 Prompts"""

FILTER_SYSTEM = """你是一个严谨的事实核查员。你的任务是评估提供的搜索片段，判断其是否包含能支撑研究维度的**实质性证据**。

## 核心任务
1. **语义判定**：该片段是否真正包含有效信息？（而非仅仅是关键词匹配）。
2. **证据提取**：如果是有效的，请提取出最硬核的事实摘要。

## 判定标准 (keep=true)
- 包含具体的统计数据、日期、法律条文、或技术细节。
- 包含机构/专家的明确观点或官方声明。
- 能够解答研究问题中的某个具体侧面。

## 摘要要求 (summary)
- **拒绝模棱两可**：不要写“本文讨论了...”，直接写“A公司2023年营收为X，同比增长Y”。
- **保留血肉**：严禁省略任何**数字、专有名词、时间点**。
- **字数控制**：摘要长度控制在 150-300 字，确保后续节点有足够的证据支撑。

## 输出要求
你必须先在 <thinking> 标签内简要分析该片段对研究维度的贡献值，然后严格按照以下 JSON 数组格式在 <json> 标签内输出。

<json>
[
    {{
        "index": 0,
        "keep": true | false,
        "reason": "简述该片段提供的核心证据点",
        "summary": "高密度事实摘要..."
    }}
]
</json>
"""

FILTER_HUMAN = """<query>{query}</query>
<intent>{intent_type}</intent>
<results>
{items_json}
</results>"""
